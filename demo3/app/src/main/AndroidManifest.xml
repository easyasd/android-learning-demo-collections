<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demo3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity5_底部顶部导航"></activity>
        <activity
            android:name=".Activity4_侧页"
            android:label="@string/title_activity_4"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity3_Activity2" />
        <activity android:name=".Activity3_单双页" />
        <activity android:name=".Activity2_多语言_多尺寸" />
        <activity android:name=".Activity1_屏幕旋转" />
    </application>

</manifest>